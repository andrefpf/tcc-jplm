stages:
  - build
  - test

image: gcc
before_script:
 - apt-get update --yes
 - apt-get install --yes cmake

build:
  stage: build
  script:
    - mkdir build && cd build; cmake ..; make -j
  artifacts:
    paths:
      - build/*

test:
  stage: test
  script:
    - cd build && ctest